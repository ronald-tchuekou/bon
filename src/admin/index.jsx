import React, { useEffect } from 'react'

export default function Admin () {

    useEffect(() => {
    }, [])

    const content = {
        padding: '30px',
        overflowX: 'hidden',
        overflowY: 'auto',
        maxHeight: '100vh'
    }

    return (
        <div style={content}>

        </div>
    );

}
